{
    "extends": "../../tsconfig.client.json",
    "compilerOptions": {
        "rootDir": "src",
        "types": ["node", "vitest/globals"]
    },
    "include": ["src/**/*.ts"],
    "exclude": ["build", "node_modules"]
}
